# Leave empty when the UI is served from the same host as the API (e.g. EB full-stack
# deploy via scripts/build-eb-frontend.sh). For Amplify or static hosting on another domain,
# set this to your HTTPS API origin with no trailing slash.
VITE_API_BASE_URL=
